# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(port_proxyTaskLib)
ADD_LIBRARY(${PORT_PROXY_TASKLIB_NAME} SHARED 
    ${PORT_PROXY_TASKLIB_SOURCES})

add_dependencies(${PORT_PROXY_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${PORT_PROXY_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PORT_PROXY_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PORT_PROXY_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PORT_PROXY_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PORT_PROXY_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PORT_PROXY_TASKLIB_HEADERS}
    DESTINATION include/orocos/port_proxy)

